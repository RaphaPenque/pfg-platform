services:
  - type: web
    name: pfg-platform
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: pfg-data
      mountPath: /data
      sizeGB: 1

services:
  - type: web
    name: pfg-platform
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: pfg-platform-db
          property: connectionString
